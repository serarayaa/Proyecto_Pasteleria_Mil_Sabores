<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cache directory para imágenes temporales -->
    <cache-path name="images" path="images/" />

    <!-- Files directory para fotos de perfil -->
    <files-path name="profile_photos" path="profile_photos/" />

    <!-- External cache para compatibilidad con algunos dispositivos -->
    <external-cache-path name="external_images" path="images/" />
</paths>

